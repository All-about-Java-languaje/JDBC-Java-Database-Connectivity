#This file content the key and values necessary for we connect to Oracle Database, but we want, could set the values
#for connect to another database, like MySQL

driver = oracle.jdbc.driver.OracleDriver
url = jdbc:oracle:thin:@(DESCRIPTION=(ADDRESS_LIST=(ADDRESS=(PROTOCOL=TCP)(HOST=localhost)(PORT=1521)))(CONNECT_DATA=(SERVICE_NAME=xepdb1)))
user = HR
pass = hr